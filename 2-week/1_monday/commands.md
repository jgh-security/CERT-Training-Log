## 1_monday 실습 상세 분석 보고서

---

## 1. 네트워크 인터페이스 및 서비스 점검
- **실습 목표:** 현재 시스템의 네트워크 설정 및 활성 연결 상태 확인
- **주요 명령어:** `ip a`, `sudo netstat -antp`
- **실습 내용:** `sudo` 권한을 이용한 상세 네트워크 프로세스 및 IP 주소 분석

### 주요 분석 결과
* **인터페이스 정보:** `192.168.200.81` 주소가 활성화되어 외부 통신 가능 상태임을 확인.
* **보안 점검 (ESTABLISHED):**
    - 외부 접속(`192.168.200.1`)은 모두 정상적인 `sshd-session`으로 확인됨.
    - 분석 결과, 관리자(Host PC)의 정상적인 원격 접속으로 판명됨.
* **서비스 점검 (LISTEN):**
    - 불필요할 수 있는 포트(`3389`, `9090` 등)가 열려 있으나, 시스템 기본 서비스(`gnome-remote`, `systemd`)에 의한 것임을 식별함.

### 수행 기록
- [x] 네트워크 설정 및 활성 연결 화면 캡처 및 업로드 완료
- [x] 프로세스 ID(PID)와 프로그램명 매칭 분석 완료

---

## 2. 실습 환경 정보 (Attacker & Target)
* **공격자(Kali) IP:** `192.168.200.80`
    - **확인 방법:** Kali 터미널에서 `ip a` 실행 및 캡처 완료.
* **대상(Target) IP:** `192.168.200.81`
    - **현재 상태:** 공격자(`...80`)로부터의 연결 흔적은 현재 없음 (**Clean 상태**).

---

## 3. 최종 요약
1. **데이터 확보:** `Target` 및 `Attacker` 각각의 네트워크 정보 캡처 완료.
2. **무결성 확인:** 실습 시작 시점의 시스템은 외부 침입 없이 깨끗함을 데이터로 증명함.
